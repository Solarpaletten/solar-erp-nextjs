# 🏗️ SOLAR ERP — ПОЛНАЯ ФАЙЛОВАЯ СТРУКТУРА

## Каноническая архитектура SolarNetJS v3

---

## 📁 APP (FRONTEND)

```
app/
│
├── layout.tsx                          # Root layout
├── page.tsx                            # Landing page (/)
├── globals.css                         # Global styles
│
│
│ ═══════════════════════════════════════════════════════════════
│ 🔐 УРОВЕНЬ 0: AUTH (Аутентификация)
│ ═══════════════════════════════════════════════════════════════
│
├── auth/
│   ├── layout.tsx                      # Auth layout (центрирование)
│   ├── login/
│   │   └── page.tsx                    # /auth/login
│   ├── register/
│   │   └── page.tsx                    # /auth/register
│   └── reset-password/
│       └── page.tsx                    # /auth/reset-password
│
│
│ ═══════════════════════════════════════════════════════════════
│ 👤 УРОВЕНЬ 1: DASHBOARD (Личный кабинет)
│ ═══════════════════════════════════════════════════════════════
│
├── dashboard/
│   ├── layout.tsx                      # Dashboard layout (sidebar)
│   ├── page.tsx                        # /dashboard — Overview
│   │
│   ├── companies/
│   │   ├── page.tsx                    # /dashboard/companies — Список компаний
│   │   └── new/
│   │       └── page.tsx                # /dashboard/companies/new — Создать
│   │
│   ├── invoices/
│   │   └── page.tsx                    # /dashboard/invoices — Счета SaaS
│   │
│   ├── profile/
│   │   └── page.tsx                    # /dashboard/profile — Мой профиль
│   │
│   └── settings/
│       └── page.tsx                    # /dashboard/settings — Настройки
│
│
│ ═══════════════════════════════════════════════════════════════
│ 🏢 УРОВЕНЬ 2: COMPANY (ERP-ядро)
│ ═══════════════════════════════════════════════════════════════
│
├── company/
│   └── [companyId]/
│       ├── layout.tsx                  # Company layout (sidebar + header)
│       ├── page.tsx                    # /company/[id] — Company Dashboard
│       │
│       │ ─────────────────────────────────────────────────────
│       │ 👥 КЛИЕНТЫ
│       │ ─────────────────────────────────────────────────────
│       ├── clients/
│       │   ├── page.tsx                # /company/[id]/clients
│       │   ├── new/
│       │   │   └── page.tsx            # /company/[id]/clients/new
│       │   └── [clientId]/
│       │       └── edit/
│       │           └── page.tsx        # /company/[id]/clients/[clientId]/edit
│       │
│       │ ─────────────────────────────────────────────────────
│       │ 📦 ТОВАРЫ
│       │ ─────────────────────────────────────────────────────
│       ├── products/
│       │   ├── page.tsx                # /company/[id]/products
│       │   ├── new/
│       │   │   └── page.tsx
│       │   └── [productId]/
│       │       └── edit/
│       │           └── page.tsx
│       │
│       │ ─────────────────────────────────────────────────────
│       │ 🏭 СКЛАД
│       │ ─────────────────────────────────────────────────────
│       ├── warehouse/
│       │   ├── page.tsx                # /company/[id]/warehouse — Overview
│       │   ├── stock/
│       │   │   └── page.tsx            # Остатки товаров
│       │   └── movements/
│       │       └── page.tsx            # Движения (приход/расход)
│       │
│       │ ─────────────────────────────────────────────────────
│       │ 🛒 ПОКУПКИ (закупки у поставщиков)
│       │ ─────────────────────────────────────────────────────
│       ├── purchases/
│       │   ├── page.tsx                # /company/[id]/purchases
│       │   ├── new/
│       │   │   └── page.tsx
│       │   └── [purchaseId]/
│       │       ├── page.tsx            # Просмотр
│       │       └── edit/
│       │           └── page.tsx
│       │
│       │ ─────────────────────────────────────────────────────
│       │ 💰 ПРОДАЖИ (клиентам)
│       │ ─────────────────────────────────────────────────────
│       ├── sales/
│       │   ├── page.tsx                # /company/[id]/sales
│       │   ├── new/
│       │   │   └── page.tsx
│       │   └── [saleId]/
│       │       ├── page.tsx
│       │       └── edit/
│       │           └── page.tsx
│       │
│       │ ─────────────────────────────────────────────────────
│       │ 🏦 БАНК / ФИНАНСЫ
│       │ ─────────────────────────────────────────────────────
│       ├── bank/
│       │   ├── page.tsx                # /company/[id]/bank — Overview
│       │   ├── accounts/
│       │   │   ├── page.tsx            # Банковские счета
│       │   │   └── new/
│       │   │       └── page.tsx
│       │   └── transactions/
│       │       ├── page.tsx            # Транзакции
│       │       └── new/
│       │           └── page.tsx
│       │
│       │ ─────────────────────────────────────────────────────
│       │ 📊 ОТЧЁТЫ
│       │ ─────────────────────────────────────────────────────
│       ├── reports/
│       │   ├── page.tsx                # /company/[id]/reports — Overview
│       │   ├── vat/
│       │   │   └── page.tsx            # PVM отчёт
│       │   ├── profit/
│       │   │   └── page.tsx            # Прибыль/убыток
│       │   ├── balance/
│       │   │   └── page.tsx            # Баланс
│       │   └── cashflow/
│       │       └── page.tsx            # Движение денег
│       │
│       │ ─────────────────────────────────────────────────────
│       │ ⚙️ НАСТРОЙКИ КОМПАНИИ
│       │ ─────────────────────────────────────────────────────
│       └── settings/
│           ├── page.tsx                # /company/[id]/settings
│           ├── profile/
│           │   └── page.tsx            # Реквизиты компании
│           ├── users/
│           │   └── page.tsx            # Сотрудники
│           └── permissions/
│               └── page.tsx            # Роли и права
│
│
│ ═══════════════════════════════════════════════════════════════
│ 🔌 API (BACKEND)
│ ═══════════════════════════════════════════════════════════════
│
└── api/
    │
    │ ─────────────────────────────────────────────────────
    │ 🔐 AUTH API
    │ ─────────────────────────────────────────────────────
    ├── auth/
    │   ├── login/
    │   │   └── route.ts                # POST /api/auth/login
    │   ├── register/
    │   │   └── route.ts                # POST /api/auth/register
    │   ├── logout/
    │   │   └── route.ts                # POST /api/auth/logout
    │   └── me/
    │       └── route.ts                # GET /api/auth/me
    │
    │ ─────────────────────────────────────────────────────
    │ 👤 DASHBOARD API (уровень пользователя)
    │ ─────────────────────────────────────────────────────
    ├── dashboard/
    │   ├── companies/
    │   │   └── route.ts                # GET/POST /api/dashboard/companies
    │   ├── invoices/
    │   │   └── route.ts                # GET /api/dashboard/invoices
    │   └── profile/
    │       └── route.ts                # GET/PUT /api/dashboard/profile
    │
    │ ─────────────────────────────────────────────────────
    │ 🏢 COMPANY API (уровень компании)
    │ ─────────────────────────────────────────────────────
    └── company/
        └── [companyId]/
            │
            ├── route.ts                # GET/PUT /api/company/[id]
            │
            ├── clients/
            │   ├── route.ts            # GET/POST
            │   └── [clientId]/
            │       ├── route.ts        # GET/PUT/DELETE
            │       └── copy/
            │           └── route.ts    # POST copy
            │
            ├── products/
            │   ├── route.ts
            │   └── [productId]/
            │       └── route.ts
            │
            ├── warehouse/
            │   ├── route.ts            # GET stock overview
            │   ├── stock/
            │   │   └── route.ts        # GET stock levels
            │   └── movements/
            │       └── route.ts        # GET/POST movements
            │
            ├── purchases/
            │   ├── route.ts
            │   └── [purchaseId]/
            │       └── route.ts
            │
            ├── sales/
            │   ├── route.ts
            │   └── [saleId]/
            │       └── route.ts
            │
            ├── bank/
            │   ├── accounts/
            │   │   ├── route.ts
            │   │   └── [accountId]/
            │   │       └── route.ts
            │   └── transactions/
            │       ├── route.ts
            │       └── [transactionId]/
            │           └── route.ts
            │
            ├── reports/
            │   ├── vat/
            │   │   └── route.ts
            │   ├── profit/
            │   │   └── route.ts
            │   ├── balance/
            │   │   └── route.ts
            │   └── cashflow/
            │       └── route.ts
            │
            └── settings/
                ├── route.ts            # GET/PUT company settings
                ├── users/
                │   └── route.ts        # GET/POST company users
                └── permissions/
                    └── route.ts        # GET/PUT permissions
```

---

## 📁 SUPPORT FOLDERS (вне app/)

```
project-root/
│
├── app/                    # ← Вся структура выше
│
├── components/             # UI компоненты
│   ├── ui/                 # Базовые (Button, Input, Modal)
│   ├── auth/               # Формы логина/регистрации
│   ├── dashboard/          # Компоненты личного кабинета
│   └── company/            # Компоненты ERP
│       ├── clients/
│       ├── products/
│       ├── warehouse/
│       ├── purchases/
│       ├── sales/
│       ├── bank/
│       └── reports/
│
├── hooks/                  # Custom React hooks
│   ├── useAuth.ts
│   ├── useCompany.ts
│   ├── useClients.ts
│   ├── useProducts.ts
│   └── ...
│
├── lib/                    # Утилиты
│   ├── prisma.ts           # Prisma client
│   ├── auth.ts             # JWT helpers
│   └── api/                # API client functions
│       ├── clients.ts
│       ├── products.ts
│       └── ...
│
├── config/                 # Конфигурации
│   ├── clients/
│   │   └── columnsConfig.ts
│   └── products/
│       └── columnsConfig.ts
│
├── types/                  # TypeScript типы
│   ├── auth.ts
│   ├── company.ts
│   ├── clients.ts
│   ├── products.ts
│   └── index.ts
│
├── docs/                   # Документация
│   ├── ARCHITECTURE.md
│   └── API.md
│
├── prisma/                 # База данных
│   ├── schema.prisma
│   └── migrations/
│
├── public/                 # Статика
│
├── package.json
├── tsconfig.json
├── tailwind.config.ts
└── next.config.js
```

---

## 🔄 USER FLOW (Путь пользователя)

```
┌──────────────────────────────────────────────────────────────────────────┐
│                                                                          │
│   1️⃣  ГОСТЬ                                                              │
│       │                                                                  │
│       ▼                                                                  │
│   /auth/register ──► Регистрация                                         │
│       │                                                                  │
│       ▼                                                                  │
│   /auth/login ─────► Вход по email + password                            │
│       │                                                                  │
│       │  ✅ JWT token в cookie                                           │
│       │                                                                  │
│   2️⃣  ▼  ПОЛЬЗОВАТЕЛЬ                                                    │
│       │                                                                  │
│   /dashboard ──────► Личный кабинет                                      │
│       │                                                                  │
│       ├──► /dashboard/companies ──► Вижу свои компании                   │
│       │         │                                                        │
│       │         ├──► [Создать компанию] ──► /dashboard/companies/new     │
│       │         │                                                        │
│       │         └──► [Войти в Company A] ─────────┐                      │
│       │                                           │                      │
│       ├──► /dashboard/invoices ──► Мои счета SaaS │                      │
│       │                                           │                      │
│       └──► /dashboard/profile ──► Мой профиль     │                      │
│                                                   │                      │
│   3️⃣                                              ▼  СОТРУДНИК КОМПАНИИ   │
│                                                   │                      │
│                               /company/17 ────────┘                      │
│                                   │                                      │
│                                   ▼                                      │
│                           Company Dashboard                              │
│                                   │                                      │
│       ┌───────────┬───────────┬───┴───┬───────────┬───────────┐         │
│       ▼           ▼           ▼       ▼           ▼           ▼         │
│   /clients   /products   /warehouse  /purchases  /sales    /bank        │
│                                                                          │
│       └───────────────────────┬───────────────────────────────┘         │
│                               │                                          │
│                               ▼                                          │
│                          /reports                                        │
│                               │                                          │
│                               ▼                                          │
│                      📊 VAT, Profit, Balance                             │
│                                                                          │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 📋 СВОДКА

| Уровень | URL prefix | Назначение | Layout |
|---------|------------|------------|--------|
| 0 | `/auth/*` | Вход в систему | Auth (центр) |
| 1 | `/dashboard/*` | Личный кабинет | Dashboard (sidebar) |
| 2 | `/company/[id]/*` | ERP компании | Company (full ERP) |

---

**SolarNetJS Canon v3** ☀️
