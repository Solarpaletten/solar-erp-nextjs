C=>D **Принял! Сначала смотрю project knowledge и docs.**Вижу текущую структуру! Сейчас создаю **детальный README для миграции**.---

## C=>L **ГОТОВО! Детальная инструкция для миграции** ✅

---

### 📋 СОЗДАНО 3 ФАЙЛА:

| Файл | Назначение |
|------|------------|
| **MIGRATION-README.md** | Полная инструкция с объяснениями (как для школьника) |
| **QUICK-COMMANDS.md** | Только команды (copy-paste) |
| **migrate.sh** | Интерактивный скрипт (спрашивает подтверждение) |

---

### 🚀 КАК ИСПОЛЬЗОВАТЬ

#### Вариант 1: Читать README и выполнять пошагово
```bash
# Открыть и читать
cat MIGRATION-README.md
```

#### Вариант 2: Копировать команды из QUICK-COMMANDS
```bash
# Просто копируй нужные блоки
cat QUICK-COMMANDS.md
```

#### Вариант 3: Запустить скрипт
```bash
# Сделать исполняемым
chmod +x migrate.sh

# Запустить
./migrate.sh
```

---

### 📍 КРАТКИЙ ПЛАН МИГРАЦИИ

```
1. Бэкап          →  cp -r src/ src_backup/
2. Создать папки  →  mkdir -p app/company/[companyId]/...
3. Копировать     →  cp "Full structure/..." app/...
4. Перенести      →  cp src/... → app/...
5. Импорты        →  sed -i '' 's|@/src/|@/|g'
6. Удалить        →  rm -rf src/
7. Собрать        →  pnpm build
8. Тестировать    →  pnpm dev
9. Коммит         →  git commit + push
```

---

### ⚠️ ВАЖНО ПЕРЕД НАЧАЛОМ

1. **tsconfig.json** — нужно изменить paths:
```json
{
  "compilerOptions": {
    "baseUrl": ".",
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

2. **Бэкап есть** — если что-то сломается, можно откатить

---

**D=>L — Инструкция готова! Можно начинать миграцию!** 🚀☀️