cd /Users/asset/Documents/IT\ project/solar-erp-nextjs/frontend/src/app/api/itsolar

# 1. POST companies - ФИКСИРОВАННЫЙ URL
cat > account/companies/route.ts << 'EOF'
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    console.log('🔄 [Frontend] Creating company:', body);

    const response = await fetch('http://localhost:3001/api/itsolar/account/companies', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    });

    const data = await response.json();

    if (!response.ok) {
      console.log('❌ Backend error:', data);
      return NextResponse.json(data, { status: response.status });
    }

    console.log('✅ Company created');
    return NextResponse.json(data, { status: 201 });
  } catch (error: any) {
    console.error('❌ Error:', error);
    return NextResponse.json({ error: 'Failed' }, { status: 500 });
  }
}
EOF

# 2. GET stats - ФИКСИРОВАННЫЙ URL
cat > account/companies/stats/route.ts << 'EOF'
import { NextRequest, NextResponse } from 'next/server';

export async function GET() {
  try {
    const response = await fetch('http://localhost:3001/api/itsolar/account/companies/stats');
    const data = await response.json();
    return NextResponse.json(data, { status: response.status });
  } catch (error: any) {
    return NextResponse.json({ error: 'Failed' }, { status: 500 });
  }
}
EOF

# 3. POST switch-to-company - ФИКСИРОВАННЫЙ URL
cat > account/switch-to-company/route.ts << 'EOF'
import { NextRequest, NextResponse } from 'next/server';

export async function POST(request: NextRequest) {
  try {
    const body = await request.json();
    const response = await fetch('http://localhost:3001/api/itsolar/account/switch-to-company', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(body),
    });
    const data = await response.json();
    return NextResponse.json(data, { status: response.status });
  } catch (error: any) {
    return NextResponse.json({ error: 'Failed' }, { status: 500 });
  }
}
EOF

echo "✅ Все proxy с фиксированным URL созданы!"

# Проверь
cat account/companies/route.ts | grep "fetch"